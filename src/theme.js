import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palatte: {
    primary: {
      main: "#764abc",
    },
  },

  background: {
    default: "#ffffff",
  },
});

export default theme;
